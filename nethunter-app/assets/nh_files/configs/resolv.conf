# NetHunter DNS resolver configuration
# Replaced at runtime with /etc/resolv.conf from the host

nameserver 8.8.8.8
nameserver 8.8.4.4
nameserver 1.1.1.1

# Optional: local search domain
# search local
